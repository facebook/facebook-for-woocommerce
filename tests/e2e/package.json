{
  "name": "facebook-for-woocommerce-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for Facebook for WooCommerce product creation and synchronization",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.39.0",
    "dotenv": "16.3.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
